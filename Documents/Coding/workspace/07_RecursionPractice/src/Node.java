
public class Node {
	String info;
	Node next;
	public Node(String s, Node link) {
            info = s;
            next = link;
        }

	/**
	 * Create a list whose first node contains a String representing n, whose
	 * nodes are in reverse order, and in which for all k in [1..n] there are k
	 * occurrences of a node containing k.
	 * 
	 * @param n
	 *            is the number used to control creation of the list
	 * @return the first node of the list created
	 */
	public static Node makeReversed(int n) {
		// TODO complete makeReversed
		return null;
	}

	public static void main(String[] args) {

	}

}
